# Q3/Q5/Q6 数据问题全面修复计划

## 问题总览

| # | 问题 | 影响 | 严重度 |
|---|------|------|:--:|
| A | **Q3 `debt_ratio` 字段取错**（baostock balance_data[6] = 负债率同比变化，不是资产负债率） | Q3 Step 5/扩展D/跨行业OLS 全部负债率结论（148/400 错值） | 🔴 |
| B | **Q3 `rev_growth` 格式不一致**（akshare col[31] 百分比形式，均值乱飞） | Q3 营收增速 t 检验不可信，与 Q5/Q6 不可比 | 🟡 |
| C | **Q5/Q6 `np_growth` 混入百分比极值**（11+29 只 baostock 给了百分比而非小数） | t 检验均值被拉偏 | 🟡 |
| D | **Q5/Q6 `roe` 混合格式**（10+23 只百分比混入） | notebook 已清洗（/100），但负 ROE 股合并为 0 丢信息 | 🟢 已缓解 |

---

## 方案 A：`debt_ratio` 修复

### 首选：双源 fallback（akshare indicator → financial_abstract）

| 步骤 | 内容 |
|------|------|
| A1 | Q3/Q5/Q6 三条基金 CSV 同时修 |
| A2 | **源 A**：`akshare.stock_financial_analysis_indicator` col[61]（资产负债率%）。主板/创业板全覆盖（Q5/Q6 已验证 100%），科创板 688 返回 NoneType |
| A3 | **源 B（fallback，仅 688）**：`akshare.stock_financial_abstract` 取"资产负债率"行（已验证：`688793 → 61.99%`）。逐行索引取值，值取 `20251231` 列 ÷ 100 |
| A4 | 两种源均不覆盖的股票 → 标记 NaN，markdown 记录缺失原因和数量 |
| A5 | 重跑 Q3 notebook → 所有负债率相关结论自动更新 |

**预估覆盖率**：Q3 ~98%+（688 约 200 只，其中 financial_abstract 应覆盖绝大多数），Q5/Q6 100%。

**风险**：
- `stock_financial_abstract` 返回 80 个指标，需精确匹配"资产负债率"行名（中文字段名，编码可能漂移）
- 688 中有部分极新上市的股票任何 API 都没有数据 → 最终仍需少量 NaN

### 备选方案 1：只用 akshare indicator，688 不管了

删除 Q3 `debt_ratio` 因子，markdown 标注"688 主板数据不可用，负债率因子在微盘研究中移除"。代价：失去 Q3 完整的因子矩阵。

### 备选方案 2：baostock 重新探索

逐一检查 baostock `query_balance_data` 的全部字段，确认是否存在任何可用字段能还原真实资产负债率。已排查结果：字段[6]≠资产负债率，其余字段（[3]-[8]）均不匹配。**此路径已死。**

---

## 方案 B：`rev_growth` 格式统一

### 首选：Q3 统一替换为 baostock `growth_data[7]`

| 步骤 | 内容 |
|------|------|
| B1 | 对 Q3 400 只股，逐只通过 baostock `query_growth_data`(2025Q4→2024Q4 fallback) 取 `growth_data[7]`（YoYOperRev = 营收同比增长率） |
| B2 | 覆盖 Q5/Q6 已验证的基础（800 只正常股 98%+ 成功率） |
| B3 | 极值 winsorize：\|rev_growth\| > 10 → 保留但 markdown 标注"可能有数据质量问题" |
| B4 | 统一后，Q3/Q5/Q6 三套数据同源同格式，五分钟位可直接对比 |

**风险**：688 科创板中 baostock 可能也有极值（已观察 -1048% 等），导致均值仍被拉偏。处理方式：不剪尾（保持数据原貌），但在 markdown 标注极端值数量和对均值的影响。

**如果没有极值，替代**：median-based MWU 已是非参检验，不受极端值影响。关键是 t 检验需要标注"受 N 个极端值影响，参考 MWU 结果"。

### 备选方案：不修

保持 Q3 原有 akshare 值，在 markdown 标注格式不一致，Q3 vs Q5/Q6 营收增速对比需带免责声明。代价：三市值对比矩阵缺一列，最重要的答辩素材受损。

---

## 方案 C：`np_growth` 极值清洗

### 首选：预处理 winsorize

| 步骤 | 内容 |
|------|------|
| C1 | Q5/Q6 的 `np_growth`，\|value\| > 5 → 标记为 `np.nan`（不可信的百分比格式） |
| C2 | 阈值依据：微盘最大 npm 增长约 91%（Q3 当前值），中盘最大合理值 ~500% → 5x 是保守阈值 |
| C3 | 或者更保守：保留 < 1 的（小数形式）作为干净集，> 1 的（百分比）另行标注，两组分别跑 t 检验，检验方向是否一致 |
| C4 | Notebook 里增加 `np_growth` 质量报告行："N 只极端值已排除" |

### 备选方案：不剪，双检验

保留所有值，但跑两组对比——(a) 全量 t 检验 (b) 去掉 \|np_growth\| > 5 后的 t 检验。如果方向一致 → 可信；如果方向反转 → 警告。代价：notebook 复杂化。

---

## 方案 D：`roe` 格式问题

### 首选：不修，markdown 标注

Notebook 已有清洗（`>1` 的 /100），负值 clamp 到 0。这个处理对微盘/中盘足够合理（ROC 负值本身就是真实信息→合并不影响排序）。

在 Cell 6 markdown 加一行："ROE 已清洗：百分比转小数 + 负值合并为 0。清洗前 Q5 有 29 只异常值（10 只 >1 + 19 只 <0）。"

---

## 执行顺序

```
1. 方案 A：修复 Q3 debt_ratio  — 用 akshare indicator + financial_abstract fallback
2. 方案 B：修复 Q3 rev_growth  — 全部替换为 baostock growth_data[7]
3. 方案 C：清洗 Q5/Q6 np_growth — winsorize |x|>5
4. 方案 D：Q5/Q6 roe 标注      — 仅 markdown 补充（不改代码）
5. 全量验证：Q3+Q5+Q6 Run All   — 确保所有 t 检验/矩阵/热力图输出一致
6. 更新所有结论 markdown        — Q3 Step 5/Step 8 + 扩展D + Q5/Q6 Step 10
```

---

## 验证清单

| 检查项 | 预期 |
|--------|------|
| Q3 `debt_ratio` 覆盖率 | ≥ 95% |
| Q3 `debt_ratio` 范围 | 0-100%（转小数后 0-1） |
| Q3 `debt_ratio` 均值/中位数 | ~25-35%（正常小微盘资产负债率） |
| Q3 `rev_growth` 格式 | decimal，中位数 ~0-0.1（5% 左右正常） |
| Q3/Q5/Q6 `rev_growth` 可比性 | 三组同源（baostock），可直接对比 |
| Q5/Q6 `np_growth` 极值 | 排除后 < 5 个残留极值 |
| Q3 负债率 t 检验 | 应与之前不同（之前是污染值） |
| 三市值对比矩阵 | 全部 7 因子 × 3 市值层可填充 |
